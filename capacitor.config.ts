import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Pet-Store-Manager',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
